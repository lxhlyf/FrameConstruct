<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--不起作用是因为toolbar 设置了   android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"-->
        <item name="colorControlNormal">@color/black</item>
    </style>

    <style name="home_bottom_tab_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">10sp</item>
        <item name="android:drawablePadding">1sp</item>
        <item name="android:textColor">@color/tab_indicator_text</item>
    </style>
</resources>
